<Window x:Class="TicTacToe.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TicTacToe"
        mc:Ignorable="d"
        Closing="Window_Closing"
        Title="TicTacToe" Height="600" Width="600" SizeChanged="Window_SizeChanged" KeyDown="Window_KeyDown">
    <Grid x:Name="Main">
        <Grid.RowDefinitions>
            <RowDefinition Height="2*" />
            <RowDefinition Height="0.1*" />
        </Grid.RowDefinitions>

        <!--ToolTip-->
        <DockPanel Grid.Row="1">

            <Label Content="A játék resetelése: CTRL+R" DockPanel.Dock="Right" />
            <Label Content="Menü: CTRL+M" DockPanel.Dock="Left" />
        </DockPanel>
        <!--Játéktér-->
        <Grid Name="Field" VerticalAlignment="Center" Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
        </Grid>
    </Grid>
</Window>